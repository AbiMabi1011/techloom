{
  "name": "techloom-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "deploy": "npx wrangler pages deploy frontend/dist --project-name=techloom",
    "start:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
